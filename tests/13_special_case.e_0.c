/*
 * Testing special case of having a complex expression
 * at the left-hand side of an assignment expression
 */
main () {
    let x = 1;
    let y = 5;
    // (let z = x + y; if (z = 0) { x } else { y }) = 7;
}
