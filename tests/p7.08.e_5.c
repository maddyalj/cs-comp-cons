/*
 * Testing let, while, asg, op and seq
 */

main () {
    const START = 1;
    let i = 20;
    let sum = 30;
    if (!START) {
        while (i) {
            if (i - 15) { 0 } else { i = 10 }
            sum = sum - i;
            i = i - 1;
        }
        sum;
    } else {
        5;
    }
}
